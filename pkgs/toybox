# Fetch package
wget -c http://www.landley.net/toybox/bin/toybox-x86_64 -O src/$pkg

# Install package
pushd src/
chmod +x $pkg
cp $pkg $root/bin
pushd $root/bin
ln -s toybox ifconfig
ln -s toybox nc
popd
popd
